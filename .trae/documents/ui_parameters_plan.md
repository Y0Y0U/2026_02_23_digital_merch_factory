# UI 参数全量整理与修改计划

本计划旨在生成一份详尽的 Markdown 文档，包含 `properties.py` 中所有 UI 参数的完整定义信息，供您查看和直接修改。

## 1. 目标

生成名为 `UI_PARAMETERS.md` 的详细参考文档，列出插件所有参数及其完整属性（类型、范围、默认值、描述等），作为配置中心。

## 2. 待执行步骤

### 步骤 1: 深度解析属性定义

* 读取 `e:\b-2_code\2026_02_23_digital_merch_factory\properties.py`。

* 提取所有 `bpy.props` 定义的完整参数集，包括：

  * **基础信息**：变量名、UI 显示名称 (`name`)、描述 (`description`)。

  * **核心值**：默认值 (`default`)。

  * **约束条件**：最小值 (`min`)、最大值 (`max`)、精度 (`precision`)。

  * **类型特有**：

    * **枚举 (Enum)**: 所有选项列表 (`items`)。

    * **布尔 (Bool)**: 开关状态。

    * **字符串/路径**: 子类型 (`subtype`)。

### 步骤 2: 生成全量参数表格

* 在项目根目录创建 `UI_PARAMETERS.md`。

* 按功能模块分类（核心、高级、生成、底座、材质等）。

* 使用扩展表格格式，确保信息无遗漏：

| 变量名             | UI 名称   | 类型/样式       | 默认值 (请修改此列) | 范围/选项                     | 描述           |
| :-------------- | :------ | :---------- | :---------- | :------------------------ | :----------- |
| `thickness_mm`  | 厚度 (mm) | 浮点数 (Float) | `3.0`       | Min: `0.0`, Max: `inf`    | 板材厚度         |
| `origin_mode`   | 原点位置    | 枚举 (Enum)   | `LEFT`      | `LEFT`: 左下角`CENTER`: 底部中心 | 选择生成模型的原点位置  |
| `auto_stand_up` | 自动立起    | 开关 (Bool)   | `True`      | `True` / `False`          | 生成后自动旋转 90 度 |

* **样式说明**：

  * **开关 (Bool)**: 对应复选框。

  * **枚举 (Enum)**: 对应下拉菜单。

  * **浮点/整数**: 对应滑动条或数值输入框。

### 步骤 3: 用户修改与应用

* 您可以在 `UI_PARAMETERS.md` 的 **"默认值"** 列直接修改数值或选项。

* 修改完成后通知我，我将读取该文档并批量更新 `properties.py` 中的代码。

## 3. 预期结果

* 生成一份包含所有参数细节的 `UI_PARAMETERS.md` 文件。

* 您可以清晰地看到每个参数的 UI 表现形式、取值范围和当前默认值，并进行精确修改。

